<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous" />
</head>

<body>
  <%- include('../partials/footer') %>
    <div class="container">
      <div class="mt-3">
        <% if(errors.length>0){ %>
          <% for(let i=0; i<errors.length; i++){ %>
            <div class="alert alert-warning alert-dismissible fade show" role="alert">
              <%= errors[i] %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
            <% } %>
              <% } %>
      </div>
      <div>
        <div class="row">
          <div class="col-md-12 mt-5">
            <form id="otpForm" action="/auth/request-otp" method="post">
              <div class="form-group">
                <label for="email">Email</label>
                <small class="form-text text-muted">
                  &nbsp;&nbsp;( Please use institute email only )
                </small>
                <div class="d-flex">
                  <input type="text" name="email" class="form-control" id="email" placeholder="john.doe@iiitg.ac.in"
                    value="<%= email %>" pattern=".*@iiitg\.ac\.in" required />
                  &nbsp;&nbsp;&nbsp;&nbsp;
                  <button type="submit" class="btn btn-warning">Request OTP</button>
                </div>
              </div>
            </form>
            <br>
            <% if (email.length!=0) { %>
              <form action="/auth/login" method="post">
                <div class="form-group">
                  <label for="otp">One Time Password</label>
                  <small class="form-text text-muted">
                    &nbsp;&nbsp;( Enter the 6 digit OTP )
                  </small>
                  <input type="hidden" name="email" id="email" required value="<%= email %>" />
                  <input type="text" name="otp" class="form-control" id="otp" placeholder="Eg. 284173" required />
                  <br>
                  <button type="submit" class="btn btn-primary btn-lg btn-block" role="button">LOGIN</button>
                </div>
              </form>
              <% } %>
          </div>
        </div>
      </div>
    </div>
    </div>
    <div style="width: 100%; margin-bottom: 120px"></div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
      crossorigin="anonymous"></script>
</body>

</html>